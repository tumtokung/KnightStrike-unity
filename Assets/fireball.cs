using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class fireball : MonoBehaviour
{
    /*public float speed = 6f;
    public float SecDestroy = 0.16f;
    float startTime;
    void Start()
    {
        //startTime =Time.time;
    }

    void Update()
    {
        /*transform Translate(Vector2.rigth*speed*Time.deltaTime);
        if(Time.time - startTime >= SecDestroy){
            Destroy(this.gameObject);
        }
    }*/
}
